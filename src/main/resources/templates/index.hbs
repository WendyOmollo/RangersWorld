{{#partial "content"}}
    <div class="overlay2">
    <div class="container ">

                    {{#if animals }}
                        {{#each animals}}
                            <!--Table-->
                            <table id="tablePreview" class="table table-hover table-striped">
                                        <tbody>
                                        <tr>
                                            <td class="text-light">{{name}}</td>
                                        </tr>
                                        </tbody>
                            </table>
                                    {{/each}}
                                {{/if}}



        </div>
    </div>
{{/partial}}
{{> layout.hbs}}